name: Windows RDP

on: workflow_dispatch

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Start RDP
        run: |
          echo "Windows machine started"





- name: Keep alive
  run: |
    echo "RDP running"
    timeout /t 21600
